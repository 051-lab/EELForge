{
  "name": "eelforge",
  "version": "0.1.0-alpha.3",
  "private": true,
  "type": "module",
  "description": "Local-first EEL2 DSP architecture and AI-agent prompt workbench for RootlessJamesDSP.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test:compile": "node scripts/clean-test-dist.mjs && tsc -p tsconfig.test.json",
    "test": "npm run test:compile && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
